<div>
  <% if @post.get_image.present? %>
    <%= image_tag post.get_image %>
  <% end %>
  <p>タイトル：<%= @post.title %></p>
  <p>内容：<%= @post.body %></p>
  <p>投稿ユーザー画像：<%= image_tag 'no-user.PNG' %></p>
  <p>ユーザーネーム：<%= @post.user.name %></p>
  <p>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
</div>